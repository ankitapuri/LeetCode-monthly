class Solution {
public:
    int distributeCandies(vector<int>& candyType) {
      
          set<int>s1;     
        for(int i=0;i<candyType.size();i++)
        {
          s1.insert(candyType[i]);
              
        }
            
         return min(candyType.size()/2,s1.size());
     }
};
